# Pre-commit
# https://github.com/pre-commit/pre-commit

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: 3e8a8703264a2f4a69428a0aa4dcb512790b2c8c  # frozen: v6.0.0
    hooks:
      - id: check-yaml
      - id: end-of-file-fixer
      - id: trailing-whitespace
      - id: check-symlinks

  - repo: https://github.com/osinfra-io/pt-techne-pre-commit-hooks
    rev: 252311ab2f4f936637351f7ea5dd2f48d6fd15cf # frozen: v0.1.8
    hooks:
      - id: tofu-fmt
        verbose: true

      # To speed up local validation:
      # mkdir -p $HOME/.opentofu.d/plugin-cache
      # Add the following to your ~/.zshrc:
      # export TF_PLUGIN_CACHE_DIR=$HOME/.opentofu.d/plugin-cache

      - id: tofu-validate
        verbose: true

      - id: tofu-scan
        verbose: true
